<!-- Dark hero section with CSS phone mockups -->
<section
  id="hero"
  class="relative min-h-screen bg-canvas-deeper overflow-hidden flex items-center pt-16"
>
  <!-- Ambient glows -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden">
    <div
      class="absolute top-[-10%] right-[-5%] w-[700px] h-[700px] rounded-full opacity-25"
      style="background: radial-gradient(circle, #E8547A 0%, transparent 70%);"
    ></div>
    <div
      class="absolute bottom-[-10%] left-[-5%] w-[500px] h-[500px] rounded-full opacity-20"
      style="background: radial-gradient(circle, #8B7FD4 0%, transparent 70%);"
    ></div>
    <!-- Dot grid -->
    <div
      class="absolute inset-0 opacity-[0.035]"
      style="background-image: radial-gradient(circle, white 1px, transparent 1px); background-size: 28px 28px;"
    ></div>
  </div>

  <div
    class="relative max-w-6xl mx-auto px-6 py-16 lg:py-24 grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-8 items-center w-full"
  >
    <!-- Left: copy -->
    <div class="text-center lg:text-left order-2 lg:order-1">
      <!-- Label pill -->
      <div
        class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-brand/10 border border-brand/20 text-brand text-xs font-semibold tracking-label uppercase mb-6"
      >
        <span class="w-1.5 h-1.5 rounded-full bg-brand animate-pulse"></span>
        Private · Just the two of you
      </div>

      <!-- Headline -->
      <h1
        class="font-display text-[2.6rem] sm:text-5xl lg:text-[3.6rem] leading-[1.1] text-white mb-5 tracking-[-0.02em]"
      >
        Stay close.<br />
        <span
          class="bg-gradient-to-r from-brand via-brand-light to-accent bg-clip-text text-transparent"
          >Still Us.</span
        >
      </h1>

      <!-- Subtitle -->
      <p class="text-white/55 text-lg leading-relaxed mb-8 max-w-[480px] mx-auto lg:mx-0">
        Your private space — just the two of you. Shared to-dos, a synced calendar,
        real-time mood sharing, and messages that arrive exactly when they should.
      </p>

      <!-- Store buttons -->
      <div
        class="flex flex-col sm:flex-row gap-3 justify-center lg:justify-start mb-10"
      >
        <a
          href="#download"
          class="inline-flex items-center gap-3 px-5 py-3.5 rounded-2xl bg-white text-ink font-semibold hover:bg-white/90 active:bg-white/80 transition-all duration-200 shadow-[0_8px_30px_rgba(255,255,255,0.12)] group"
        >
          <i
            class="fab fa-apple text-xl group-hover:scale-110 transition-transform duration-200"
          ></i>
          <div class="text-left leading-none">
            <p class="text-[10px] text-ink/45 font-normal mb-1">Download on the</p>
            <p class="text-sm font-semibold">App Store</p>
          </div>
        </a>
        <a
          href="#download"
          class="inline-flex items-center gap-3 px-5 py-3.5 rounded-2xl bg-white/8 border border-white/12 text-white font-semibold hover:bg-white/14 active:bg-white/10 transition-all duration-200 group"
        >
          <i
            class="fab fa-google-play text-lg text-brand group-hover:scale-110 transition-transform duration-200"
          ></i>
          <div class="text-left leading-none">
            <p class="text-[10px] text-white/40 font-normal mb-1">Get it on</p>
            <p class="text-sm font-semibold">Google Play</p>
          </div>
        </a>
      </div>

      <!-- Social proof -->
      <div
        class="flex items-center gap-4 justify-center lg:justify-start text-sm text-white/40"
      >
        <div class="flex -space-x-2">
          <div
            class="w-7 h-7 rounded-full bg-gradient-to-br from-brand to-brand-dark border-2 border-canvas-deeper"
          ></div>
          <div
            class="w-7 h-7 rounded-full bg-gradient-to-br from-accent to-accent-dark border-2 border-canvas-deeper"
          ></div>
          <div
            class="w-7 h-7 rounded-full bg-gradient-to-br from-rose-400 to-accent border-2 border-canvas-deeper"
          ></div>
          <div
            class="w-7 h-7 rounded-full bg-gradient-to-br from-brand-dark to-purple-600 border-2 border-canvas-deeper"
          ></div>
        </div>
        <span
          >Loved by <strong class="text-white/70 font-semibold">10,000+</strong> couples worldwide</span
        >
      </div>
    </div>

    <!-- Right: phone mockups -->
    <div
      class="order-1 lg:order-2 relative flex justify-center items-end min-h-[480px] lg:min-h-[560px]"
    >
      <!-- Background phone: Messages (tilted right, behind) -->
      <div class="phone-wrapper absolute right-4 sm:right-10 lg:right-2 bottom-0 z-10" style="transform: rotate(7deg) translateY(-20px);">
        <div class="phone-frame" style="width:170px;height:340px;">
          <div class="phone-screen screen-light">
            <div class="pill-notch"></div>
            <div class="px-3 pt-2 pb-3">
              <p class="screen-eyebrow text-ink/40">MESSAGES</p>
              <p class="screen-title text-ink">Inbox</p>
              <!-- Message rows -->
              <div class="mt-2 space-y-0">
                <div class="msg-row border-b border-ink/6">
                  <div class="msg-avatar" style="background:linear-gradient(135deg,#E8547A,#8B7FD4)">
                    <span class="text-[10px]">💌</span>
                  </div>
                  <div class="flex-1 min-w-0">
                    <div class="flex justify-between items-baseline">
                      <p class="msg-name text-ink">From Alex</p>
                      <p class="msg-time text-ink/30">2d</p>
                    </div>
                    <p class="msg-preview text-ink/45">🔒 Time-lock · Dec 25</p>
                  </div>
                </div>
                <div class="msg-row border-b border-ink/6">
                  <div class="msg-avatar bg-accent/15">
                    <span class="text-[10px]">🔐</span>
                  </div>
                  <div class="flex-1 min-w-0">
                    <div class="flex justify-between items-baseline">
                      <p class="msg-name text-ink">From Alex</p>
                      <p class="msg-time text-ink/30">5d</p>
                    </div>
                    <p class="msg-preview text-ink/45">Password protected</p>
                  </div>
                </div>
                <div class="msg-row">
                  <div class="msg-avatar bg-brand/10">
                    <span class="text-[10px]">💬</span>
                  </div>
                  <div class="flex-1 min-w-0">
                    <div class="flex justify-between items-baseline">
                      <p class="msg-name text-ink">From Alex</p>
                      <p class="msg-time text-ink/30">1w</p>
                    </div>
                    <p class="msg-preview text-ink/45">I'm thinking of you…</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Main phone: Home screen (straight, front) -->
      <div class="phone-wrapper relative z-20" style="transform: rotate(-3deg);">
        <div class="phone-frame" style="width:200px;height:400px;">
          <div class="phone-screen screen-dark">
            <div class="pill-notch bg-black/90"></div>
            <div class="px-3 pt-1.5 pb-3 h-full flex flex-col">
              <div class="status-row">
                <span class="text-white/50">9:41</span>
                <span class="flex gap-1 text-white/50">
                  <!-- Wifi icon -->
                  <svg viewBox="0 0 24 24" fill="currentColor" class="w-3 h-3">
                    <path d="M1.5 8.5a13 13 0 0 1 21 0M5.5 12.5a7 7 0 0 1 13 0M9.5 16.5a3 3 0 0 1 5 0M12 21h.01"/>
                  </svg>
                  <!-- Battery icon -->
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="w-3.5 h-3">
                    <rect x="1" y="6" width="16" height="12" rx="2"/>
                    <path d="M23 13v-2"/>
                    <rect x="3" y="8" width="10" height="8" rx="1" fill="currentColor" stroke="none"/>
                  </svg>
                </span>
              </div>
              <!-- Greeting -->
              <p class="text-[10px] font-semibold text-white/35 tracking-[0.07em] uppercase mt-2">Home</p>
              <p class="text-sm font-semibold text-white mt-0.5 mb-3">Good morning, Emma 👋</p>

              <!-- Mood card -->
              <div class="dark-card mb-2">
                <p class="card-eyebrow">MOODS</p>
                <div class="flex justify-around items-center mt-2 py-1">
                  <div class="text-center">
                    <p class="text-xl leading-none">😌</p>
                    <p class="text-[8px] text-white/35 mt-1 font-medium">You · Calm</p>
                  </div>
                  <div class="text-brand text-base opacity-60">♥</div>
                  <div class="text-center">
                    <p class="text-xl leading-none">🥰</p>
                    <p class="text-[8px] text-white/35 mt-1 font-medium">Alex · Loving</p>
                  </div>
                </div>
              </div>

              <!-- Tasks widget -->
              <div class="dark-card mb-2">
                <div class="flex justify-between items-center">
                  <p class="card-eyebrow">TASKS</p>
                  <span class="text-[9px] text-brand font-semibold">2 open</span>
                </div>
                <div class="flex items-center gap-1.5 mt-2">
                  <span class="w-1.5 h-1.5 rounded-full bg-brand/50 flex-shrink-0"></span>
                  <p class="text-[10px] text-white/60 truncate">Buy anniversary flowers 🌸</p>
                </div>
                <div class="flex items-center gap-1.5 mt-1.5">
                  <span class="w-1.5 h-1.5 rounded-full bg-accent/50 flex-shrink-0"></span>
                  <p class="text-[10px] text-white/60 truncate">Book dinner for Saturday</p>
                </div>
              </div>

              <!-- Letters strip -->
              <div class="dark-card">
                <p class="card-eyebrow">LETTERS</p>
                <div class="flex gap-2 mt-2">
                  <div class="letter-dot" style="background:linear-gradient(135deg,#E8547A,#8B7FD4)"></div>
                  <div class="letter-dot" style="background:linear-gradient(135deg,#8B7FD4,#6355B0)"></div>
                  <div class="letter-dot bg-white/8 border border-white/10"></div>
                  <div class="letter-dot bg-white/5 border border-white/8"></div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll cue -->
  <div
    class="absolute bottom-6 left-1/2 -translate-x-1/2 flex flex-col items-center gap-1.5 text-white/25 animate-bounce"
  >
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="1.5"
      stroke-linecap="round"
      class="w-5 h-5"
    >
      <path d="M12 5v14M5 12l7 7 7-7"></path>
    </svg>
  </div>
</section>

<style>
  .phone-wrapper {
    filter: drop-shadow(0 40px 60px rgba(0, 0, 0, 0.55));
  }

  .phone-frame {
    border-radius: 36px;
    border: 7px solid #2A2535;
    overflow: hidden;
    background: #2A2535;
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.05);
  }

  .phone-screen {
    width: 100%;
    height: 100%;
    position: relative;
    overflow: hidden;
  }

  .screen-dark {
    background: linear-gradient(170deg, #1A1A2E 0%, #15152A 100%);
  }

  .screen-light {
    background: linear-gradient(170deg, #ffffff 0%, #fde8f0 100%);
  }

  .pill-notch {
    width: 70px;
    height: 18px;
    background: #0a0a12;
    border-radius: 9px;
    margin: 6px auto 0;
    display: block;
  }

  .status-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 8px;
    font-weight: 600;
    padding: 2px 0;
  }

  .screen-eyebrow {
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    margin-top: 6px;
  }

  .screen-title {
    font-size: 13px;
    font-weight: 700;
    margin-top: 2px;
    margin-bottom: 8px;
  }

  /* Dark screen card styles */
  .dark-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 12px;
    padding: 9px 10px;
  }

  .card-eyebrow {
    font-size: 8px;
    font-weight: 700;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.3);
  }

  .letter-dot {
    width: 26px;
    height: 26px;
    border-radius: 50%;
  }

  /* Light screen message styles */
  .msg-row {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 7px 0;
  }

  .msg-avatar {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .msg-name {
    font-size: 10px;
    font-weight: 600;
    line-height: 1;
  }

  .msg-time {
    font-size: 8px;
  }

  .msg-preview {
    font-size: 9px;
    margin-top: 2px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
</style>
